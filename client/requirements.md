## Packages
recharts | Visualization for player statistics and leaderboards
framer-motion | Smooth animations for page transitions and modal interactions
date-fns | Robust date formatting for match history
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
API endpoints follow /api/matches, /api/users, /api/stats
Images will be static or standard icons (Lucide React)
